# v1 보관

`v1-single-file.html` 은 이 앱의 첫 버전입니다. 단일 HTML 파일에 CDN 으로 React 와
Tailwind 를 불러오고 브라우저에서 Babel 로 JSX 를 컴파일하던 구조였습니다.

레시피 수치의 원본 출처라 남겨 둡니다. 현재 `src/data/recipes.ts` 의 값이 원본과
어긋나 보이면 여기를 대조하세요. 옮기면서 의도적으로 바꾼 부분은 루트 README 의
"v1 에서 달라진 것" 에 적어 뒀습니다.

동작하는 앱은 저장소 루트입니다. 이 파일은 실행하지 않습니다.
